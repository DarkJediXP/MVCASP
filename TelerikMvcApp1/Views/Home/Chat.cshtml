<!-- Nothing is wrong with my project there is just an error when trying to get access to my database-->
@{
    ViewBag.Title = "Chat";
}



<style type="text/css">
    textarea {
        width: 300px;
        height: 100px;
        max-height: 100px;
        scrollbar-base-color: #ff8c00;
        scrollbar-arrow-color: white;
        resize: none;
        overflow: auto;  
    }
    

</style>
<body>
    <div class="textarea">
        <table>
            <tr>
                <td>

                </td>
                <td>
                    <h2 align="right">Clan</h2>
                </td>
            </tr>
            <tr>
                <td>
                    <!--<textarea readonly id="discussion" rows="7" cols="50"></textarea>-->
                    <div id='fake_textarea' class="DivToScroll DivWithScroll" style="float: left;" contenteditable></div>
                </td>
                <td>
                    <div class="textarea">                  
                        <textarea style="float: right;" readonly id="Clan"></textarea>
                    </div>
                </td>
            </tr>
            <tr>
                <td>
                    <input type="hidden" id="displayname" />
                    <br />
                    <input type="text" id="message" />
                    <input type="button" id="sendmessage" value="Send" />
                </td>
            </tr>
        </table>
        
    </div>
    
    
</body>


<script type="text/javascript">
    $(function () {
        //When a user enters the NakamaChat the user will display under Clan 
        $('#Clan').val($('#Clan').val() + "@ViewBag.name\n");
        // Declare a proxy to reference the hub.
        var chat = $.connection.chatHub;
        // Create a function that the hub can call to broadcast messages.
        chat.client.broadcastMessage = function (name, message) {
            // Html encode display name and message.
            var encodedName = $('<div />').text(name).html();
            var encodedMsg = $('<div />').text(message).html();
            // Add the message to the page.
            //$('#discussion').append('<li><strong>' + encodedName
            //   + '</strong>:&nbsp;&nbsp;' + encodedMsg + '</li>');
            $('#fake_textarea').append('<p class="triangle-border left">'+ 'Buttttt it could be any element you want.' + '</p>');
           // $('#discussion').val($('#discussion').val() + encodedName + ": " + encodedMsg + "\n");
        };
        console.log("@ViewBag.name");
        // Get the user name and store it to prepend to messages.
        // $('#displayname').val(prompt('Enter your name:', ''));
        $('#displayname').val("@ViewBag.name");
        console.log($('#displayname').val());
        // Set initial focus to message input box.
        $('#message').focus();
        // Start the connection.
        $.connection.hub.start().done(function () {
            $('#sendmessage').click(function () { // on click listner for the send message button
                // Call the Send method on the hub.
                chat.server.send($('#displayname').val(), $('#message').val());
                console.log($('#message').val());
                // Clear text box and reset focus for next comment.
                $('#message').val('').focus();
            });
        });
    });
</script>